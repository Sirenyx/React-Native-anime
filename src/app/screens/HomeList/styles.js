import { StyleSheet, BackHandler } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1, 
        alignItems: 'center',
    },
    separator: {
        height: 2,
        margin: 10,
        backgroundColor: '#000'
    }
});

export default styles;